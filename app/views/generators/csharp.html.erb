using Newtonsoft.Json;
using System;

namespace RingCentral.Models.<%= namespace %>
{
    public class <%= class_name %>
    {
      <% properties.each do |prop| %>
        [JsonProperty("<%= prop.name %>")]
        public <%= csharp_type prop %> <%= prop.name.camelize %>;
      <% end %>
    }
}
